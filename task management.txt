TO DO
- add optional arguments: n_iter (default 2000?) and set_seed (default current time)
- add pseudocode for iter_panel
- add complete example run to repo

LATER GOALS
- Efficiency: split score_mips() into several functions so small changes for panel rescoring
- Usability: If no initial panel filepath supplied, run MIPGEN, then immediately run MIPGEN_scoring after (no need to convert .txt to .csv)
- Explore different RNG algorithm options?
- Figure out "convergence" metric for unif_loss

COMPLETED
4/29 commit "iterative algorithm implemented via iter_panel(); fix to score_mips()"
- fixed score_mips: add final region for each feature to unif_loss
- code up functions for iterative algorithm (naive, no checks on validity of MIPs, assuming MIPgen scoring will help take care of that until we can put constraints in):
	- perturbation of MIPs
	- rescoring of panel after perturbation
	- comparison of new unif_loss to previous unif_loss
	- if better, keep change; if worse, go back to previous panel
	- output loss score AND change to panel for each perturbation
- output final panel in the same format as MIPgen's final picked MIPs via the print_details() function

4/24 commit "update README.md"
- update README to clarify how to get a .csv file for MIPGEN_scoring from the output picked_mips.txt file from MIPGEN

4/23 commit "update documentation; score_mips() implementation from pseudocode.txt"
- added GNU-GSL dependency, updated makefile so GNU could be used in mipgen.cpp
- implemented code to get loss of uniformity metric from initial panel (score_mips() function)
	- spot check of MIP scores, MIP lambdas, base pair probability for binom, and binom pdf revealed no issues
- output loss of uniformity to ALLPANELS file
- all changes in mipgen.cpp enclosed by "additions from ellen" / "end additions" comments
- troubleshooting having regular MIPgen run when no initial panel filepath supplied - working for EMW (have KD check)
- pseudocode uploaded in .txt file